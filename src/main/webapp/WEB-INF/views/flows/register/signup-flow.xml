<?xml version="1.0" encoding="UTF-8"?>
<flow xmlns="http://www.springframework.org/schema/webflow"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.springframework.org/schema/webflow
                          http://www.springframework.org/schema/webflow/spring-webflow.xsd">

	<var name="user" class="com.inovisionsoftware.model.User"/>
	
	<!--  Start state (is normally the first view-state defined in the XML) -->
	<view-state id="personal" view="flows/register/signup-personal" model="user">
		<transition on="billing" to="billingState"></transition>
	</view-state>

	<view-state id="billingState" view="flows/register/signup-billing"></view-state>

	<view-state id="confirm" view="flows/register/signup-confirm"></view-state>
	
	<!-- End States -->
	<end-state id="homeEnd" view="externalRedirect:contextRelative:/"></end-state>	
	<end-state id="success" view="flows/register/signup-success"></end-state>
	
	
	<!-- Global transition applicable to all views -->
	<global-transitions>
		<!-- on is the id of this transistion specified in _eventId_home  -->
		<!-- to is the state id to which we want to transition to (in this case it is end state home specified in end-state) -->
		<transition on="home" to="homeEnd"></transition>
	</global-transitions>
	
</flow>